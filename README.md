# Inline-error audit — image assets

Screenshots referenced by the inline-error parity audit PR. Not part of the
source tree; this orphan branch exists only so the PR body can embed them.

See `docs/product/inline-error-audit.md` on `audit/inline-error-parity`.
